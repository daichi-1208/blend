<br>
<div class="row">
    <div class="col-5">
        <%= attachment_image_tag @user, :image, size:"180x180", fallback: "no_image.jpg" %>
    </div>
    <div class="col-7">
        <h5><%= @user.name %> さんのページ</h5>
        <table class="table">
            <tbody>
                <tr>
                    <td><%= @user.introduction %></td>
                </tr>
                <tr>
                    <td>カクテル投稿数 <i class="fas fa-cocktail fa-fw"></i><%= @user.posts.count %></td>
                </tr>
                <tr>
                    <td>いいねをもらった数 <i class="far fa-star fa-fw"><%= @user.favorites.count %></td>
                </tr>
            </tbody>        
        </table>
    </div>
</div>
<br>
<div class="text-center">
    <h4 class="text-center">このユーザーが投稿したカクテル</h4>
    <br>
    <div class="row">
        <% @user.posts.each do |post| %>
            <div class="col-4 mb-1 ">
                <%= link_to theme_post_path(:theme_id => @theme.id, :id => post.id) do %><%= attachment_image_tag post, :image, size:"140x140", fallback: "no_image.jpg" %>
                    <% end %>
                <%= post.name %><br>
                <i class="far fa-star fa-fw star-color"></i></span><%= post.favorites.count%>
            </div>
        <% end %>
    </div>
</div>