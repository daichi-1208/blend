user_themes#index

<h2>お題一覧</h2>
<% @themes.each do |theme| %>
    <%= link_to theme_path(theme.id) do %><%= attachment_image_tag theme, :image, :fill, 200, 150, format: "jpg" %>
    <% end %>
    <%= theme.name %>
<% end %>